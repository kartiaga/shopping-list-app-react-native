import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        gap: 5,
        paddingTop: 12,
        paddingBottom: 16,
    },
    title: {
        fontSize: 12,
        fontWeight: 600
    }
    
})