import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: "#2d9700ff",
        height: 48,
        width: "100%",
        borderRadius: 8,
        alignItems: "center",
        justifyContent: "center"
    },
    title: {
        color: "#FFF",
        fontSize: 14,
        fontWeight: 600
    }
})